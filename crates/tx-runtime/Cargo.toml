[package]
name = "tx-runtime"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at
# https://doc.rust-lang.org/cargo/reference/manifest.html

[features]
tx32 = []
nan-boxing = []
debug-features = []
gc-stress = []

[dependencies]
